#pragma once
#include "surface.h"
#include "game.h"

namespace Tmpl8
{
    class Effects {
    public:
        void drawWaterSplash(Surface* screen, float deltaTime, vec2 player_pos);
    private:
    };
}
